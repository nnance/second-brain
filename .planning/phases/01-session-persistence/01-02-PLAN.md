---
phase: 01-session-persistence
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [src/sessions/file-store.test.ts, src/sessions/store.test.ts]
---

<objective>
Add tests verifying session persistence works correctly across file operations and simulated restarts.

Purpose: Ensure sessions actually survive process restarts and file operations are reliable.
Output: Comprehensive test coverage for the file-based session store.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-session-persistence/01-01-SUMMARY.md
@src/sessions/file-store.ts
@src/sessions/store.ts
@src/sessions/store.test.ts

**Tech stack:** Node.js native test runner (`node:test`), `assert` module
**Testing patterns:**
- Co-located test files (*.test.ts)
- Use temp directories for file tests
- `before()`/`after()` for setup/cleanup
- `beforeEach()` to reset state
- Descriptive test names starting with "should"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file store unit tests</name>
  <files>src/sessions/file-store.test.ts</files>
  <action>
Create tests for the file-store module:

1. Test `loadSessions()`:
   - Returns empty Map when file doesn't exist
   - Parses existing JSON file correctly
   - Converts ISO date strings back to Date objects
   - Handles corrupt JSON gracefully (returns empty Map, logs error)

2. Test `saveSessions()`:
   - Creates file when it doesn't exist
   - Writes valid JSON
   - Handles Date serialization (Date → ISO string)
   - Atomic write works (use temp file path to verify)

Use temp directory for tests:
```typescript
const testStorePath = "/tmp/test-sessions.json";
```

Set `SESSION_STORE_PATH` environment variable for tests or mock config.

Cleanup: Remove test file in `after()` hook.
  </action>
  <verify>`npm test` passes with new tests</verify>
  <done>file-store.test.ts exists with tests for load/save, all pass</done>
</task>

<task type="auto">
  <name>Task 2: Add persistence integration tests to store.test.ts</name>
  <files>src/sessions/store.test.ts</files>
  <action>
Add a new describe block for persistence integration tests:

```typescript
describe("persistence", () => {
  // Test that createSession persists to file
  // Test that sessions survive simulated restart (clear Map, call initSessionStore, verify data)
  // Test that updateSession persists changes
  // Test that deleteSession removes from file
});
```

Key test: "should survive simulated restart"
1. Create a session
2. Wait briefly for async save to complete
3. Clear the in-memory Map directly (simulating process death)
4. Call initSessionStore()
5. Verify session is restored with correct data

Use a dedicated test store path to avoid conflicts with other tests.
  </action>
  <verify>`npm test` passes with all tests including new persistence tests</verify>
  <done>store.test.ts has persistence tests, simulated restart test passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npm test` passes all tests (including new persistence tests)
- [ ] `npm run lint` passes
- [ ] Restart simulation test verifies data survives
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- File store has unit test coverage
- Store has integration tests for persistence
- Simulated restart test proves sessions survive
- Phase 1 complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-session-persistence/01-02-SUMMARY.md`
</output>
