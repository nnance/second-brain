# Required: Absolute path to Obsidian vault
VAULT_PATH=/Users/yourname/Documents/SecondBrain

# Required: Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Claude model (default: claude-sonnet-4-20250514)
CLAUDE_MODEL=claude-sonnet-4-20250514

# Optional: Log level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info

# Optional: Session timeout in milliseconds (default: 3600000 = 1 hour)
SESSION_TIMEOUT_MS=3600000

# Optional: Maximum retry attempts for transient errors (default: 3)
MAX_RETRIES=3

# Optional: Initial retry delay in milliseconds (default: 1000, uses exponential backoff)
RETRY_DELAY_MS=1000
